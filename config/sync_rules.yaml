bucket_definitions:
  global:
    data:
      - SELECT * FROM orders
  user_data:
    parameters:
      - SELECT (request.jwt() ->> 'user_id')::INTEGER AS user_id
    data:
      - SELECT * FROM orders WHERE user_id = bucket.user_id